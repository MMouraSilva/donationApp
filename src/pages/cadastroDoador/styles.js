import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    checkBoxView: {
        flexDirection: 'row',
        marginBottom: 80,
        marginTop: 30,
        alignItems: 'center',
    },

    checkBoxText: {
        marginLeft: 10
    }

});

export default styles;