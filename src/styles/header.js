import { StyleSheet } from 'react-native';

const header = StyleSheet.create({

    headerRight: {
        marginRight: 20
    },

    headerLeft: {
        marginLeft: 20
    }
})

export default header;