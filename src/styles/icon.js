import { StyleSheet } from 'react-native';

const icon = StyleSheet.create({
    
    nextIcon: {
        fontSize: 20,
        textAlign: 'center',
        color: '#FED500',
        marginTop: "1%"
    },

    createIcon: {
        fontSize: 20,
        textAlign: 'center',
        color: '#FED500'
    }
});

export default icon;